# ============================================
# Supabase Configuration
# ============================================
# 1. https://supabase.com 에서 새 프로젝트 생성
# 2. Project Settings → Database → Connection string (URI) 복사
# 3. Project Settings → API → URL 과 anon key 복사
# 4. 이 파일을 .env.local로 복사 후 값 입력:
#    cp .env.example .env.local

# PostgreSQL 연결 (Drizzle ORM에서 사용)
# ⚠️ Transaction mode 포트(6543)가 아닌 Session mode 포트(5432) 사용
DATABASE_URL=postgresql://postgres.[YOUR-PROJECT-REF]:[YOUR-PASSWORD]@aws-0-ap-northeast-2.pooler.supabase.com:5432/postgres

# Supabase Client (브라우저에서 사용)
NEXT_PUBLIC_SUPABASE_URL=https://[YOUR-PROJECT-REF].supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=[YOUR-ANON-KEY]

# Supabase Service Role (서버에서만 사용, 절대 클라이언트에 노출 금지)
SUPABASE_SERVICE_ROLE_KEY=[YOUR-SERVICE-ROLE-KEY]

# ============================================
# Feature Flags
# ============================================
# AI 견적 진단 기능 (v1.0) — true: 활성화 / false: 숨김
NEXT_PUBLIC_ENABLE_AI_DIAGNOSIS=false
